diff --git a/pc/CMakeLists.txt b/pc/CMakeLists.txt
index 1c9f378..2c8c29e 100644
--- a/pc/CMakeLists.txt
+++ b/pc/CMakeLists.txt
@@ -3,15 +3,24 @@ project(ac_pc C CXX)
 
 option(BELLPAD_MACOS_BUNDLE "Build the playable Apple Silicon baseline as Bellpad.app" OFF)
 option(BELLPAD_AURORA_CORE_PROBE "Compile the game core with Aurora integration enabled" OFF)
-option(BELLPAD_AURORA_LINK_PROBE "Link the game core to Aurora for macOS" OFF)
+option(BELLPAD_AURORA_LINK_PROBE "Link the game core to Aurora on Apple platforms" OFF)
+if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+    set(BELLPAD_LEGACY_PC_DEFAULT OFF)
+else()
+    set(BELLPAD_LEGACY_PC_DEFAULT ON)
+endif()
+option(BELLPAD_BUILD_LEGACY_PC "Build the SDL2/OpenGL desktop executable" ${BELLPAD_LEGACY_PC_DEFAULT})
 set(BELLPAD_AURORA_SOURCE_DIR "" CACHE PATH "Path to the pinned Aurora source checkout")
+set(BELLPAD_IOS_INFO_PLIST "" CACHE FILEPATH "Product Info.plist for an iOS Aurora game bundle")
+set(BELLPAD_PRODUCT_SOURCE_DIR "" CACHE PATH "Path to Bellpad's product platform sources")
 if(APPLE)
     enable_language(OBJC)
+    enable_language(OBJCXX)
 endif()
 
 if(BELLPAD_AURORA_LINK_PROBE)
     if(NOT APPLE)
-        message(FATAL_ERROR "The Bellpad Aurora link probe currently supports macOS only")
+        message(FATAL_ERROR "The Bellpad Aurora link probe currently supports Apple platforms only")
     endif()
     if(NOT EXISTS "${BELLPAD_AURORA_SOURCE_DIR}/CMakeLists.txt")
         message(FATAL_ERROR "Set BELLPAD_AURORA_SOURCE_DIR to the pinned Aurora checkout")
@@ -20,8 +29,16 @@ if(BELLPAD_AURORA_LINK_PROBE)
     set(AURORA_ENABLE_DVD OFF CACHE BOOL "" FORCE)
     set(AURORA_ENABLE_CARD ON CACHE BOOL "" FORCE)
     set(AURORA_CACHE_USE_ZSTD OFF CACHE BOOL "" FORCE)
-    set(AURORA_DAWN_PROVIDER package CACHE STRING "" FORCE)
-    set(AURORA_SDL3_PROVIDER system CACHE STRING "" FORCE)
+    if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+        set(AURORA_DAWN_PROVIDER vendor CACHE STRING "" FORCE)
+        set(AURORA_DAWN_LINKAGE static CACHE STRING "" FORCE)
+        set(AURORA_SDL3_PROVIDER vendor CACHE STRING "" FORCE)
+        set(DAWN_BUILD_PROTOBUF OFF CACHE BOOL "" FORCE)
+        set(TINT_BUILD_IR_BINARY OFF CACHE BOOL "" FORCE)
+    else()
+        set(AURORA_DAWN_PROVIDER package CACHE STRING "" FORCE)
+        set(AURORA_SDL3_PROVIDER system CACHE STRING "" FORCE)
+    endif()
     add_subdirectory("${BELLPAD_AURORA_SOURCE_DIR}" aurora)
 endif()
 
@@ -99,19 +116,22 @@ include_directories(
 # =============================================================================
 # Find SDL2
 # =============================================================================
-# Try system SDL2 first, then fall back to platform-specific locations
-if(WIN32)
+# Try system SDL2 first, then fall back to platform-specific locations.
+# The Aurora iOS target uses its own vendored SDL3 and must not probe host SDL2.
+if(BELLPAD_BUILD_LEGACY_PC AND WIN32)
     set(SDL2_DIR "${CMAKE_CURRENT_SOURCE_DIR}/lib/SDL2/cmake" CACHE PATH "SDL2 cmake dir")
 endif()
-find_package(SDL2 QUIET)
-if(NOT SDL2_FOUND)
+if(BELLPAD_BUILD_LEGACY_PC)
+    find_package(SDL2 QUIET)
+endif()
+if(BELLPAD_BUILD_LEGACY_PC AND NOT SDL2_FOUND)
     # Try pkg-config (works on both Linux and MSYS2)
     find_package(PkgConfig QUIET)
     if(PKG_CONFIG_FOUND)
         pkg_check_modules(SDL2 QUIET sdl2)
     endif()
 endif()
-if(NOT SDL2_FOUND)
+if(BELLPAD_BUILD_LEGACY_PC AND NOT SDL2_FOUND)
     if(WIN32)
         # Windows-specific fallback paths
         # MSYS2 32-bit location
@@ -149,24 +169,28 @@ if(NOT SDL2_FOUND)
             "  Or download SDL2 development libraries to ${CMAKE_CURRENT_SOURCE_DIR}/lib/SDL2")
     endif()
 endif()
-include_directories(${SDL2_INCLUDE_DIRS})
-message(STATUS "SDL2 include: ${SDL2_INCLUDE_DIRS}")
-message(STATUS "SDL2 libraries: ${SDL2_LIBRARIES}")
+if(BELLPAD_BUILD_LEGACY_PC)
+    include_directories(${SDL2_INCLUDE_DIRS})
+    message(STATUS "SDL2 include: ${SDL2_INCLUDE_DIRS}")
+    message(STATUS "SDL2 libraries: ${SDL2_LIBRARIES}")
+endif()
 
 # Fix SDL2 target for 32-bit Linux multilib builds
-if(UNIX AND NOT APPLE AND CMAKE_SIZEOF_VOID_P EQUAL 4)
+if(BELLPAD_BUILD_LEGACY_PC AND UNIX AND NOT APPLE AND CMAKE_SIZEOF_VOID_P EQUAL 4)
     include(cmake/FixSDL2-i686.cmake)
 endif()
 
 # =============================================================================
 # GLAD (OpenGL 3.3 loader - generated by glad2)
 # =============================================================================
-add_library(glad STATIC
-    ${CMAKE_CURRENT_SOURCE_DIR}/lib/glad/src/gl.c
-)
-target_include_directories(glad PUBLIC
-    ${CMAKE_CURRENT_SOURCE_DIR}/lib/glad/include
-)
+if(BELLPAD_BUILD_LEGACY_PC)
+    add_library(glad STATIC
+        ${CMAKE_CURRENT_SOURCE_DIR}/lib/glad/src/gl.c
+    )
+    target_include_directories(glad PUBLIC
+        ${CMAKE_CURRENT_SOURCE_DIR}/lib/glad/include
+    )
+endif()
 
 # =============================================================================
 # Collect game source files
@@ -479,6 +503,8 @@ if(BELLPAD_AURORA_LINK_PROBE)
         ${CMAKE_CURRENT_SOURCE_DIR}/src/pc_pad.c
         ${CMAKE_CURRENT_SOURCE_DIR}/src/pc_apple.m
         ${CMAKE_CURRENT_SOURCE_DIR}/src/pc_audio.c
+        ${CMAKE_CURRENT_SOURCE_DIR}/src/pc_texture_pack.c
+        ${CMAKE_CURRENT_SOURCE_DIR}/src/pc_keybindings.c
         ${DECOMP_ROOT}/src/static/dolphin/pad/Padclamp.c
     )
     list(APPEND BELLPAD_AURORA_SUPPORT_SOURCES
@@ -498,7 +524,6 @@ if(BELLPAD_AURORA_LINK_PROBE)
     # desktop baseline while Aurora and its dependencies retain Release flags.
     target_compile_options(ac_aurora PRIVATE -O0)
     target_link_libraries(ac_aurora PRIVATE
-        glad
         aurora::core
         aurora::main
         aurora::os
@@ -509,17 +534,56 @@ if(BELLPAD_AURORA_LINK_PROBE)
         aurora::card
         aurora::gx
         aurora::gd
-        "-framework Cocoa"
-        "-framework IOKit"
         pthread
         m
     )
+    if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+        if(NOT EXISTS "${BELLPAD_IOS_INFO_PLIST}")
+            message(FATAL_ERROR "Set BELLPAD_IOS_INFO_PLIST to Bellpad's iOS product plist")
+        endif()
+        if(NOT EXISTS "${BELLPAD_PRODUCT_SOURCE_DIR}/apple/ios/BellpadGameOverlay.mm")
+            message(FATAL_ERROR "Set BELLPAD_PRODUCT_SOURCE_DIR to the Bellpad checkout")
+        endif()
+        target_sources(ac_aurora PRIVATE
+            "${BELLPAD_PRODUCT_SOURCE_DIR}/apple/ios/BellpadGameOverlay.mm"
+            "${BELLPAD_PRODUCT_SOURCE_DIR}/src/platform/BellpadInput.cpp"
+        )
+        target_include_directories(ac_aurora PRIVATE
+            "${BELLPAD_PRODUCT_SOURCE_DIR}/apple/ios"
+            "${BELLPAD_PRODUCT_SOURCE_DIR}/src/platform"
+        )
+        target_compile_definitions(ac_aurora PRIVATE BELLPAD_IOS=1)
+        target_compile_options(ac_aurora PRIVATE
+            "$<$<COMPILE_LANGUAGE:OBJCXX>:-fobjc-arc>"
+        )
+        target_link_libraries(ac_aurora PRIVATE
+            "-framework Foundation"
+            "-framework GameController"
+            "-framework QuartzCore"
+            "-framework UIKit"
+            "-framework Metal"
+        )
+        set_target_properties(ac_aurora PROPERTIES
+            MACOSX_BUNDLE TRUE
+            MACOSX_BUNDLE_INFO_PLIST "${BELLPAD_IOS_INFO_PLIST}"
+            OUTPUT_NAME "Bellpad"
+            RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
+        )
+    else()
+        target_link_libraries(ac_aurora PRIVATE
+            "-framework Cocoa"
+            "-framework IOKit"
+        )
+    endif()
     set_target_properties(ac_aurora PROPERTIES
-        OUTPUT_NAME "BellpadAurora"
         RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
     )
+    if(NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
+        set_target_properties(ac_aurora PROPERTIES OUTPUT_NAME "BellpadAurora")
+    endif()
 endif()
 
+if(BELLPAD_BUILD_LEGACY_PC)
 add_executable(ac_pc ${AC_PC_EXECUTABLE_TYPE}
     ${PC_SOURCES}
     ${FIXNES_SOURCES}
@@ -623,3 +687,4 @@ install(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/shaders/"
     DESTINATION bin/shaders
     FILES_MATCHING PATTERN "*.vert" PATTERN "*.frag"
 )
+endif()
diff --git a/pc/include/pc_pause_menu.h b/pc/include/pc_pause_menu.h
index 705a9c3..80c439d 100644
--- a/pc/include/pc_pause_menu.h
+++ b/pc/include/pc_pause_menu.h
@@ -2,7 +2,11 @@
 #ifndef PC_PAUSE_MENU_H
 #define PC_PAUSE_MENU_H
 
+#ifdef AURORA
+#include <SDL3/SDL.h>
+#else
 #include <SDL.h>
+#endif
 
 #ifdef __cplusplus
 extern "C" {
diff --git a/pc/include/pc_platform.h b/pc/include/pc_platform.h
index feec44a..3f0ead1 100644
--- a/pc/include/pc_platform.h
+++ b/pc/include/pc_platform.h
@@ -4,9 +4,13 @@
 
 #include <stdint.h>
 
+#ifdef AURORA
+#include <SDL3/SDL.h>
+#else
 #define SDL_MAIN_HANDLED
 #include <SDL.h>
 #include <glad/gl.h>
+#endif
 
 #include <stdio.h>
 #include <stdlib.h>
diff --git a/pc/src/pc_aurora_main.c b/pc/src/pc_aurora_main.c
index 5b84fb6..a30205a 100644
--- a/pc/src/pc_aurora_main.c
+++ b/pc/src/pc_aurora_main.c
@@ -13,6 +13,9 @@
 #include <dolphin/pad.h>
 
 #include <SDL3/SDL_scancode.h>
+#include <SDL3/SDL_filesystem.h>
+#include <SDL3/SDL_hints.h>
+#include <SDL3/SDL_stdinc.h>
 
 #include <dlfcn.h>
 #include <mach-o/dyld.h>
@@ -24,6 +27,11 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#include <unistd.h>
+
+#ifdef BELLPAD_IOS
+#include "BellpadGameOverlay.h"
+#endif
 
 struct SDL_Window* g_pc_window = NULL;
 void* g_pc_gl_context = NULL;
@@ -224,12 +232,29 @@ int main(int argc, char* argv[]) {
         .mem1Size = 256 * 1024 * 1024,
         .mem2Size = 16 * 1024 * 1024,
     };
+#ifdef BELLPAD_IOS
+    SDL_SetHint(SDL_HINT_ORIENTATIONS, "LandscapeLeft LandscapeRight");
+#endif
     AuroraInfo info = aurora_initialize(argc, argv, &config);
     s_aurora_initialized = true;
     g_pc_window = info.window;
     g_pc_window_w = (int)info.windowSize.native_fb_width;
     g_pc_window_h = (int)info.windowSize.native_fb_height;
 
+#ifdef BELLPAD_IOS
+    bellpad_install_game_overlay();
+#endif
+
+    char* writable_path = SDL_GetPrefPath("Bellpad", "Bellpad");
+    if (!writable_path || chdir(writable_path) != 0) {
+        fprintf(stderr, "Bellpad could not enter its writable Application Support directory.\n");
+        if (writable_path) SDL_free(writable_path);
+        pc_platform_shutdown();
+        return 1;
+    }
+    printf("[Storage] Using %s\n", writable_path);
+    SDL_free(writable_path);
+
     PADInit();
     bellpad_configure_default_keyboard();
 
diff --git a/pc/src/pc_nes_fixnes.c b/pc/src/pc_nes_fixnes.c
index c55d5ee..7064ed1 100644
--- a/pc/src/pc_nes_fixnes.c
+++ b/pc/src/pc_nes_fixnes.c
@@ -31,9 +31,12 @@
 #undef BUTTON_LEFT
 #undef BUTTON_RIGHT
 
-/* Now include GL via SDL2 (avoid pc_platform.h which pulls in game types.h) */
+/* The legacy renderer presents the NES framebuffer through OpenGL. Aurora
+ * keeps the emulator and audio path but needs a GX/Metal presentation path. */
+#ifndef AURORA
 #include <SDL2/SDL.h>
 #include <glad/gl.h>
+#endif
 #include "fm2play.h"
 #include "audio.h"
 
@@ -89,13 +92,13 @@ extern void AIInitDMA(uintptr_t addr, uint32_t size);
 /* ======================================================================
  * GL rendering state
  * ====================================================================== */
+static int fixnes_initialized = 0;
+#ifndef AURORA
 static GLuint fixnes_shader = 0;
 static GLuint fixnes_vao = 0;
 static GLuint fixnes_vbo = 0;
 static GLuint fixnes_texture = 0;
 static GLint fixnes_tex_uniform = -1;
-static int fixnes_initialized = 0;
-
 static GLuint fixnes_compile_shader(GLenum type, const char *src) {
     GLuint s = glCreateShader(type);
     glShaderSource(s, 1, &src, NULL);
@@ -161,6 +164,7 @@ static void fixnes_init_gl(void) {
     glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_WRAP_S, GL_CLAMP_TO_EDGE);
     glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_WRAP_T, GL_CLAMP_TO_EDGE);
 }
+#endif
 
 /* ======================================================================
  * Audio buffer capture — convert fixNES APU output to stereo s16 for
@@ -376,6 +380,11 @@ void pc_fixnes_cleanup(void) {
 }
 
 void pc_fixnes_render_frame(uint16_t *fb) {
+#ifdef AURORA
+    /* TODO: upload this RGB565 framebuffer through GX so Aurora presents it
+     * through Metal. NES emulation and audio continue to run meanwhile. */
+    (void)fb;
+#else
     if (!fixnes_shader) fixnes_init_gl();
 
     /* Upload framebuffer — fixNES outputs RGB565 with COL_TEX_BSWAP
@@ -421,4 +430,5 @@ void pc_fixnes_render_frame(uint16_t *fb) {
     glBindVertexArray(0);
 
     glViewport(0, 0, win_w, win_h);
+#endif
 }
diff --git a/pc/src/pc_pause_menu.c b/pc/src/pc_pause_menu.c
index 455ead8..aaca2be 100644
--- a/pc/src/pc_pause_menu.c
+++ b/pc/src/pc_pause_menu.c
@@ -12,6 +12,17 @@
 #include <stdio.h>
 #include <string.h>
 
+#ifdef AURORA
+#undef SDLK_a
+#undef SDLK_d
+#undef SDLK_s
+#undef SDLK_w
+#define SDLK_a SDLK_A
+#define SDLK_d SDLK_D
+#define SDLK_s SDLK_S
+#define SDLK_w SDLK_W
+#endif
+
 int g_pc_paused = 0;
 
 /* Pausing is blocked while either is set: main menu (set by ac_animal_logo)
@@ -78,10 +89,18 @@ static void confirm_activate(void) {
 
 int pc_pause_menu_handle_event(const SDL_Event* e) {
     if (!g_pc_paused) return 0;
+#ifdef AURORA
+    if (e->type != SDL_EVENT_KEY_DOWN) return 0;
+#else
     if (e->type != SDL_KEYDOWN) return 0;
+#endif
     if (e->key.repeat) return 1;
 
+#ifdef AURORA
+    SDL_Keycode k = e->key.key;
+#else
     SDL_Keycode k = e->key.keysym.sym;
+#endif
 
     /* Settings page is driven by pc_settings_menu. */
     if (cur_page == PAGE_SETTINGS) {
diff --git a/pc/src/pc_typing.c b/pc/src/pc_typing.c
index a74796d..18278d4 100644
--- a/pc/src/pc_typing.c
+++ b/pc/src/pc_typing.c
@@ -36,7 +36,11 @@ int pc_typing_begin(void) {
     if (!g_pc_editor_active) return 0;
     if (!g_pc_typing_mode) {
         g_pc_typing_mode = 1;
+#ifdef AURORA
+        SDL_StartTextInput(g_pc_window);
+#else
         SDL_StartTextInput();
+#endif
         pc_typing_queue_clear();
     }
     return 1;
@@ -44,7 +48,11 @@ int pc_typing_begin(void) {
 
 void pc_typing_end(void) {
     g_pc_typing_mode = 0;
+#ifdef AURORA
+    SDL_StopTextInput(g_pc_window);
+#else
     SDL_StopTextInput();
+#endif
 }
 
 /* Map a UTF-8 character from SDL_TEXTINPUT to an AC game character code.
@@ -151,8 +159,14 @@ int pc_typing_command(int command) {
 }
 
 void pc_typing_handle_event(const SDL_Event* event) {
+#ifdef AURORA
+    if (event->type == SDL_EVENT_KEY_DOWN) {
+        SDL_Keycode key = event->key.key;
+#else
     if (event->type == SDL_KEYDOWN) {
-        if (event->key.keysym.sym == SDLK_TAB && !event->key.repeat && g_pc_editor_active) {
+        SDL_Keycode key = event->key.keysym.sym;
+#endif
+        if (key == SDLK_TAB && !event->key.repeat && g_pc_editor_active) {
             if (g_pc_typing_mode) {
                 pc_typing_end();
             } else {
@@ -161,7 +175,7 @@ void pc_typing_handle_event(const SDL_Event* event) {
         }
 
         if (g_pc_typing_mode && g_pc_editor_active) {
-            switch (event->key.keysym.sym) {
+            switch (key) {
                 case SDLK_BACKSPACE: pc_typing_command(PC_TYPING_CMD_BACKSPACE); break;
                 case SDLK_RETURN:
                 case SDLK_KP_ENTER:  pc_typing_command(PC_TYPING_CMD_ENTER); break;
@@ -174,7 +188,11 @@ void pc_typing_handle_event(const SDL_Event* event) {
         }
     }
 
+#ifdef AURORA
+    if (event->type == SDL_EVENT_TEXT_INPUT && g_pc_typing_mode && g_pc_editor_active) {
+#else
     if (event->type == SDL_TEXTINPUT && g_pc_typing_mode && g_pc_editor_active) {
+#endif
         pc_typing_commit_utf8(event->text.text);
     }
 }
diff --git a/src/static/jaudio_NES/internal/os.c b/src/static/jaudio_NES/internal/os.c
index fe3925d..ae8c813 100644
--- a/src/static/jaudio_NES/internal/os.c
+++ b/src/static/jaudio_NES/internal/os.c
@@ -4,8 +4,16 @@
 #include "jaudio_NES/sample.h"
 
 #ifdef TARGET_PC
+#ifdef AURORA
+#include <SDL3/SDL.h>
+#else
 #include <SDL.h>
+#endif
+#ifdef AURORA
+static SDL_Mutex* z_mq_mutex = NULL;
+#else
 static SDL_mutex* z_mq_mutex = NULL;
+#endif
 
 void pc_audio_mq_init(void) {
     if (!z_mq_mutex) z_mq_mutex = SDL_CreateMutex();
