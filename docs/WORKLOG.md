# Worklog

## 2026-08-03

- Established a persistent implementation goal and milestone plan.
- Audited the initially empty root repository and local references.
- Added and verified `.gitignore` coverage for `ref/Animal Crossing.iso`, other disc formats, extracted data, saves, packages, build output, signing material, and secrets.
- Inspected HarkinianPad commit `1197472…` and its restrictive integration-code licensing boundary.
- Researched and cloned ac-decomp, the current PC port, the full 64-bit migration, Aurora, ACreTeam forest, OpenCrossing Anbernic, and older macOS work.
- Inspected upstream branches, forks, issues, and PRs, especially PC-port PRs #33 and #48.
- Confirmed the current PC port is still forced to 32-bit, while PR #48 reports macOS ARM64 and end-to-end save/reload.
- Confirmed Aurora's iOS/Metal, disc, controller, RTC, and save capabilities from source.
- Confirmed ACreTeam forest already integrates Aurora services but currently disables Aurora GX and uses no-op GX stubs.
- Selected a provisional hybrid architecture and documented its decision gates.
- Installed Homebrew GCC 16.1.0 and built the pinned 64-bit fork successfully as an ARM64 Mach-O (15,380,656 bytes).
- Validated the ignored local image as `GAFE01`, revision 0, with correct GameCube magic; stored its size/hash only in `ref/.image-validation.txt`, which is ignored.
- Launched the baseline through SDL/Cocoa. It indexed 10 disc files, decompressed the REL, loaded 14,495 assets directly from the image, and opened 32 kHz stereo audio.
- Diagnosed the initial black screen: no draw calls occurred because JSystem's aligned 64-byte read of the last 56-byte file extended eight bytes past a trimmed image's EOF and the DVD shim retried forever.
- Corrected the local DVD shim to clamp reads to the declared file size and zero-fill only the aligned tail. The game then rendered the title correctly at 60 FPS with hundreds of draw calls and no GL errors.
- Packaged the ignored baseline executable temporarily as a minimal `.app` only so local UI automation could inspect its Cocoa window; no retail data was copied into the bundle.
- Reached K.K.'s new-game dialogue with correct rendering/audio. Town creation remains blocked because A redraws the first page instead of advancing.
- Reproduced a timing-dependent title transition that skipped cleanup and repeatedly panicked on an invalid arena free. Changed local panic behavior to abort with a bounded stack trace for future diagnosis.
- Confirmed the current shutdown path ignores window close, Command-Q, SIGINT, and SIGTERM, requiring SIGKILL during tests.
- Added an event-latched keyboard-button experiment in the ignored reference clone so synthesized short key events cannot fall entirely between PAD polls; this informs the future normalized mobile input layer but is not yet product code.
- Instrumented message advancement and proved the K.K. message state is coherent on ARM64 (`main=normal`, bounded cursor indices, correct cursor/continue/end branches). The earlier apparent dialogue blocker was input timing, not pointer corruption.
- Advanced through the complete K.K. introduction, Rover setup, player/town naming, train sequence, generated-town arrival, station exit, outdoor movement, Tom Nook greeting, and house-selection area.
- Validated desktop text input with the local test names `Bell` and `Cedar`; no save artifact was committed or copied.
- Promoted the DVD-tail correction and minimal keyboard-edge latch into tracked, game-data-free patches.
- Added idempotent pinned fetch and ARM64 build scripts. Tested them from a fresh ignored checkout; the resulting clean executable mounted the supported image directly, loaded 14,495 assets, initialized audio, and reached the title loop.
- Confirmed a targeted `SIGTERM` terminates the fresh scripted executable. Window-close/app-wrapper teardown remains to be reconciled with the earlier SIGKILL-only observation.
- Audited Apple Clang diagnostics across the full source tree and promoted the resulting legal, game-data-free portability work into a third tracked patch.
- Repaired native-width DVD/audio callbacks and addresses, structure-sized task copies, JKR heap/archive arithmetic, PC ARAM, retrace messages, C++ allocation signatures, Famicom buffers, and legacy declaration issues without widening guest or serialized data.
- Removed the 64-bit Clang configuration ban. Apple Clang 21.0.0 compiled all 4,000 units and linked a native ARM64 executable; a runtime smoke test visibly reached the correctly rendered title at 60 FPS.
- Rebuilt the same patched checkout in an independent directory with GCC 16.1.0. All 4,000 units compiled and the ARM64 executable linked, showing the portability changes did not regress the previous compiler path.
- Corrected the keyboard-edge patch so its handler is inside `case SDL_KEYDOWN` rather than unreachable after the window-event `break`, then retained the edge through the two `PADRead` calls used by a game input update. Repeated patch fetches stayed idempotent, both compilers rebuilt, and one normalized A edge advanced exactly one K.K. prompt.
- Added a guarded LLDB desktop button-tap helper for repeatable QA. It validates an explicit AnimalCrossing PID and feeds the existing normalized input queue; it neither injects game state nor handles copyrighted data.
- Made Apple Clang the default reproducible desktop compiler and retained explicit compiler/build-directory overrides for compatibility testing.
- Reproduced the title teardown fault under the Apple Clang build and captured a native `sample`/LLDB stack at `play_cleanup → Actor_info_delete → zelda_free → __osFree`.
- Resolved the invalid pointer to `aSTR_actor_cl + 2496`. Clang layout output proved that `SHRINE_ACTOR` is 840 bytes but each static slot was only the 832-byte `STRUCTURE_ACTOR`, so the Shrine tail overwrote the next actor's ID/name fields.
- Found current PC-port commit `7fa20d75…`, which independently repairs the same structure-pool sizing mistake on its 32-bit target. Ported that storage design using 0x400-byte host slots for 64-bit safety.
- Rebuilt the repair with Apple Clang 21 and GCC 16. Repeated automatic title teardown/reload cycles completed without the former invalid-free panic.
- Added an SDL-independent native text surface over the existing editor queue, then refactored SDL events to use it. Both Clang and GCC rebuilt after two clean patch applications; runtime QA entered `Bell` and `Cedar` across two distinct editor lifecycles.
- Added a guarded alphanumeric desktop text helper and short retry handling for transient LLDB detach races. The helper validates the exact AnimalCrossing PID and uses only the native editor API.
- Added a normalized virtual GameCube pad with buttons, both sticks, and analog triggers, plus deterministic merge rules for simultaneous physical and virtual sources. Two clean patch applications and both compiler builds passed at 4,001 units.
- Verified the pad ABI byte-for-byte through a live `PADRead`: the requested sticks/triggers produced packed bytes `0x8278d42137b30060`, including derived L/R bits. Added a guarded left-stick QA helper and verified set/clear calls.
- Corrected the desktop text helper to clear all eight editor cells before committing a replacement value, avoiding leftover characters when a destination editor is pre-populated.
- Added a frame-counted stick-pulse helper. The first implementation revealed that LLDB retains only the final repeated `breakpoint command add -o` command; direct memory inspection caught the uncleared stick. The corrected helper resumes to a one-shot `PADRead` breakpoint, clears state in the following batch command, optionally queues a button edge, and detaches with all virtual-pad bytes zero.
- Used normalized input only for gameplay actions after alignment: selected Bell's house, accepted Nook's mortgage, equipped the uniform through inventory, validated rejected planting on paving, planted seven flowers and three saplings on valid ground, and advanced to the introductions chore.
- Compiled temporary ignored ARM64 offset probes, then deleted them. Runtime structures reported Cedar has six villagers; Bell's friendship count advanced to two after complete first conversations with Peaches and Chuck. The town list identified the remaining four villager houses without modifying save data.
- Reproduced an NPC-house interaction defect. At Bunnie's house the live actor was centered at `(2060, 2100)`, normal collision stopped Bell near `z=2175`, and the original forward item sample remained outside the `0xF0A6` door unit. A one-frame position diagnostic reached `item_in_front=0xF0A6`, confirming the downstream door label path.
- The subsequent house transition crashed after 2 h 44 min. The macOS report records `EXC_BAD_ACCESS` at `0x2C` in `putLEWord`, called by `mEA_GetCardDLProgram` during `play_init`; `zelda_malloc_align` had returned null for the optional 9,612-byte e-Reader payload.
- Added patch 7 to validate the e-Reader resource and treat arena-allocation failure as an unavailable optional payload. Added patch 8 with a narrow `TARGET_PC` NPC-house approach fallback that preserves the original label test and does not change global interaction distance.
- Applied all eight patches idempotently and rebuilt both Apple Clang 21 and GCC 16 ARM64 executables. The rebuilt Clang binary mounted the ignored disc, loaded all 14,495 assets, initialized 32 kHz audio, reached a stable 60 FPS title loop, and returned from `graph_proc` after an exact-PID `SIGTERM`. End-to-end house-transition replay remains required.
- Captured a separate intermittent Rover-train `EXC_BAD_ACCESS` in `tex_content_hash → GXLoadTexObj → emu64::dirty_check → dl_G_TRIN`. A 32×16 `GX_TF_C8` texture carried low pointer `0x43C80000`; the macOS pointer helper had treated any sufficiently high low word as executable data using a speculative 256 MiB image interval.
- Replayed the same fresh setup with normalized pad input and the native editor API, crossed the exact preceding money-choice dialogue, and arrived at Cedar station. This showed the fault was allocation-layout dependent rather than a deterministic dialogue error.
- Added patch 9: macOS now derives its exact loaded Mach-O bounds from `LC_SEGMENT_64` commands, low-word recovery is range-checked even across a 4 GiB wrap, and texture/TLUT objects reject unknown low pointers before hashing or decoding.
- Probed the live Apple Clang process under LLDB. The image interval was `0x100000000–0x1025C0000`; `0x43C80000` became null, while truncated arena and executable addresses reconstructed to `0x11C47D234` and `0x1009E4020` respectively.
- Applied all nine patches idempotently, rebuilt with Apple Clang 21 and GCC 16, and ran a normal 60 FPS title smoke that returned cleanly after exact-PID `SIGTERM`.
- Corrected the gameplay harness after observing a 120 FPS `[2x]` run accelerate the simulation. All subsequent timing-sensitive evidence uses the normal 60 FPS rate; the accelerated observations are excluded from timing, audio, RTC, memory, save, and stability claims.
- Restarted the Apple Clang build without verbose PTY logging and replayed a fresh `Bell`/`Cedar` town at 60 FPS through the train, house/mortgage selection, Nook's shop, uniform equipment, and the complete planting assignment. The inventory correctly rejected paved soil, accepted grass, consumed seven flower bags and three saplings, and Nook accepted the work.
- Added a single-attach repeated-button helper for dialogue QA and lengthened transient LLDB ownership retries across the button/stick helpers. The helper separates each normalized edge by a one-shot `PADRead` breakpoint and all three scripts pass `sh -n`.
- Approached a nearby villager house in the fresh run. Its resident was outdoors, so no interior scene allocation occurred; patches 7 and 8 remain explicitly runtime-pending rather than being inferred from process survival.
- Decoded the live 7-by-10 town block-type table and matched Cedar's `RIVER_WEST_BRIDGE`, `SLOPE_HORIZONTAL`, and `BEACH_RIVER_BRIDGE` acres to their source collision grids. Used only the normalized virtual pad for movement, then crossed the generated upper-town route, descended the four-cell ramp centerline, crossed the lower beach bridge, and reached resident houses on both sides of the river.
- Re-read the six live `Animal_c::is_home` flags before each resident-house attempt. The target occupants changed to outdoors during traversal and appeared visibly next to their houses, so the run correctly records a schedule-gated test rather than claiming the patched interior path passed.
- Completed first-time resident dialogue with Purrl and Rex at the normal 60 FPS rate. Brief 59.9/low title-bar averages immediately after LLDB stops are debugger sampling artifacts; the presentation counter returns to 60.0 FPS after detach and no accelerated mode is used.
- Removed the desktop fast-forward hazard after confirming that 120 Hz advances the VI-driven simulation at 2×. Patch 10 fixes pacing at 60 Hz, removes the F3/F4 toggle, normalizes persisted high/unlimited values to 60, and rejects 120/unlimited command-line requests. Apple Clang 21 and GCC 16 ARM64 rebuilt successfully, and the CLI rejection tests returned the expected status 2.
- Zoomed out from tutorial resident-count automation and audited the repository against the full Apple-product goal. Confirmed the root still contains no macOS/iOS/iPadOS product source, making the Apple shell, Metal integration, adaptive touch/controller input, lifecycle, import, and persistence the primary path rather than further scripted desktop play.
- Built pinned Aurora `5027ed63…` on macOS ARM64 with GX enabled. Runtime logs selected Dawn's Apple M2 Metal adapter and the visible blue GX clear rendered correctly.
- Tested both Aurora iOS dependency routes. The released `dawn-ios-arm64` archive is device-only and cannot link an iOS Simulator executable. A source-Dawn Xcode-generator build compiled but did not emit the final Dawn archives; the equivalent Ninja build emitted `libwebgpu_dawn.a` and linked the complete ARM64 `IOSSIMULATOR` app.
- Added minimal bundle metadata only to the ignored generated example, then installed/launched it on an iPhone 17 Pro simulator. After terminating it and shutting down the phone, installed/launched the same build on an iPad Pro 13-inch simulator. Both visibly rendered through Metal; the iPad resizable-window result records the remaining product layout work.
- Added pinned Aurora fetch/build scripts for reproducible macOS and iOS Simulator Metal/GX probes. These contain no retail data and explicitly distinguish dependency feasibility from a Bellpad game-app pass.
- Created the first Bellpad-owned product source: a CMake-built native AppKit bundle, a universal UIKit bundle, explicit ROM-free Info.plists, and a shared C++ normalized GameCube input library.
- Added a fixed-60-Hz MetalKit presentation surface to both shells. Launched the macOS ARM64 app, visually confirmed its native Metal window, and verified normal application quit.
- Added a complete first-pass GameCube touch overlay with left/C sticks, A/B/X/Y, Z/L/R/Start, and D-pad. Touch buttons/sticks update the same normalized state used by GameController; a unit test proves merge and clear semantics.
- Installed/launched the Bellpad shell on iPhone 17 Pro Simulator, corrected shoulder/status overlap, and visually confirmed a clean safe-area layout. Stopped and shut down iPhone before starting iPad.
- The iPad Pro 13-inch Simulator launched the app in a 960×640 resizable window. The first device-class-only layout overlapped, so sizing now uses actual safe-area dimensions and scales the compact controls/font chrome for small iPad windows; the repeated screenshot is clean. Expanded iPad sizing remains a separate test gate.
- Added lifecycle clearing/presentation pause-resume and physical-controller auto-hide on real devices, while keeping simulator virtual controllers from hiding the touch QA surface. Added a reproducible Apple-shell build script.
- Added a portable disc-header validator that reads only 0x20 bytes, recognizes raw ISO/GCM, and requires GameCube magic, `GAFE01`, and revision 0. Expanded the native test with synthetic headers for valid, wrong-revision, wrong-game, and unsupported-container results; no retail bytes or fixtures are used.
- Added a validation-only `NSOpenPanel` to macOS and security-scoped `UIDocumentPickerViewController` to iOS/iPadOS. Neither path copies, bookmarks, hashes, or retains the selected image yet.
- Used Computer Use to activate both product buttons. Apple's native iPhone Files picker and macOS open sheet visibly presented and were cancelled without selecting any file. The Computer Use skill directly enabled this UI verification; command-line build evidence alone could not prove modal presentation.
- Re-centered the implementation on the whole product rather than resident-count diagnostics. Confirmed the 120 FPS overclock path was already removed: simulation remains coupled to 60 Hz, and high-refresh presentation cannot be enabled until simulation is separated.
- Added patch 11 and a reproducible build command that package the complete Apple Clang ARM64 game core as an opt-in `Bellpad.app`. The patch adds a native disc picker, explicit `--disc` API, strict GAFE01 disc 0 revision 0 checks, clean shader bundle resources, and no retail data.
- Built all 4,000 units into the app, launched its executable from an unrelated temporary directory against the ignored development image, indexed 10 disc files, loaded 14,495 assets, opened 32 kHz audio, entered `graph_proc`, and reached the completed title prompt at fixed 60 Hz. This proves a real-game macOS app bundle; it still uses SDL2/OpenGL and cwd-based saves, so Aurora/Metal convergence and Application Support persistence remain next.
- Re-ran the timing guard through the bundled executable: `--framelimit 120` and `--no-framelimit` both return status 2 with an explicit acceleration warning.
- Added patch 12 so macOS creates and enters `~/Library/Application Support/Bellpad` before settings, keybindings, and GCI access. An isolated `BELLPAD_DATA_HOME` test reported the requested directory as its save cwd; a second launch loaded the same settings and keybindings while disc and shaders still resolved correctly.
- Replaced per-patch reverse probing with an ordered hash record under the ignored checkout's `.git/` directory. The old probe became invalid once patch 12 modified an Objective-C file introduced by patch 11; the new runner bootstraps existing complete checkouts, resumes new tail patches, and rejects changed history. A fresh temporary clone accepted all twelve patches in order.
- Audited the actual 3,905 compiled game-core objects against pinned Aurora rather than using a source-name estimate. The core imports 112 GX/GD symbols; Aurora originally lacked five. Added an explicit Aurora patch implementing software-FIFO abort, texture-copy clamp state, scissor-box offset emission, source-port render-thread ownership, and hardware-counter fallback. Patched `libaurora_gx.a` rebuilt with all 112 requirements resolved.
- Compiled both GX structure definitions and found a real palette-object storage mismatch: the core allocated 16 bytes while Aurora requires 40. Patch 13 expands only the `TARGET_PC` opaque `GXTlutObj` storage, leaving the GameCube layout unchanged. The ABI audit now proves exact value-type matches and sufficient texture/palette storage, and the full 3,361-step playable macOS app rebuild linked successfully.
- Audited the full game core against the temporary OpenGL renderer and reduced eleven nonstandard imports to zero under `AURORA`. Frame begin/flush calls and diagnostic counters remain isolated to the OpenGL oracle; host-generated palette byte order is now carried through one explicit Aurora TLUT API instead of hidden renderer state.
- Added patch 14 and a reproducible 3,904-object Aurora core target. The target retains the required host/ARM64 heap, pointer, color, texture-cache, palette-change, and `GXEnd` behavior; all 111 GX/GD imports resolve and no legacy renderer hook remains. The playable ARM64 `Bellpad.app` relinked afterward as a regression check.
- Linked the complete game to pinned Aurora as a native ARM64 executable. Removed SDL2 from that target, added an SDL3/CoreAudio 32 kHz stereo adapter, resolved ten overlapping platform symbols by assigning ownership explicitly, and retained the OpenGL app as a separate behavior oracle.
- Diagnosed the first black Metal window with a native process sample: JSystem acquired an Aurora frame and then blocked forever waiting for a GameCube VI message. The Aurora path now uses synchronous begin/end boundaries plus the fixed desktop 60 Hz retrace cadence.
- Diagnosed the resulting first-input stack-check failure under LLDB. Aurora's host `PADStatus` appends a 32-bit extended-button field, making its array stride 16 bytes; the game header still allocated the 12-byte SDK form. Matching the Aurora-only ABI removed the overwrite.
- Launched the corrected target against the ignored supported image. Metal selected the Apple M2 adapter, 14,495 assets loaded, SDL3 audio opened, all archives mounted, the title actor advanced through its setup states, and the interactive menu ran at 60.0 FPS. The image is recognizable but visibly corrupted, so graphics correctness is the next blocker rather than being misreported as a rendering pass.
- Added patch 15 and `build-aurora-game-macos.sh` to reproduce the convergence target and verify that its Mach-O is ARM64, links SDL3/Metal, and does not link SDL2. The target remains an unbundled macOS integration executable until GX correctness and product-service hookup pass.
- Compared the smeared title against the bypassed JSystem frame lifecycle. The synchronous unblock had also skipped EFB clearing and display-manager finalization, while Aurora's display-copy call is intentionally a no-op. Patch 16 explicitly clears color/depth, restores the 2D port/cache boundary, and runs end-render work around Aurora's frame. A repeated title smoke remains at 60 FPS and now shows a stable black background with only the current frame; incorrect emu64 logo placement/color is the narrowed graphics blocker.
- Compared a fresh OpenGL title oracle with the same scene through Aurora. Disc palettes are byte-swapped into host `u16` values by the asset loader; Aurora patch 2 marked that provenance, but only the dynamic palette path consumed it. Aurora patch 3 centralizes byte-order normalization so static paletted textures receive the same conversion. Title trees/lettering changed from neon/opaque corruption to plausible colors while geometry remained wrong.
- Disabled Aurora's draw merge optimization after an isolated run removed the worst cross-screen polygon spikes. The current merge predicate observes GX state equality but not every reused emu64 texture/matrix dependency; correctness takes priority until those dependencies are part of the merge key.
- Connected `GXInvalidateTexAll` to Aurora's deferred static-cache clear. A pre-fix long title run eventually logged a Metal texture-allocation failure after continued texture creation. The post-fix executable held 60 Hz through a 5,000-frame smoke without that failure; this is useful regression evidence, not the required multi-hour memory soak.
- Replaced a misleading matrix hypothesis with an isolated culling comparison. The first emu64 projection/matrix capture was internally consistent; forcing culling off restored the complete title scene, proving vertex placement was sound. Changing Aurora's WebGPU front face from clockwise to counter-clockwise retained the logo, ground, trees, map, and character with normal game-requested culling. Patch 4 records the fix; visible water and K.K. dialogue/selection output now narrow the remaining renderer work to texture/alpha/TEV behavior.
- Audited input after the renderer became interactive. Aurora PAD intentionally loaded no default keyboard bindings, and Bellpad's normalized virtual-pad state still terminated in the old OpenGL PAD path. Patch 17 preserves saved Aurora mappings, installs the established desktop defaults only when absent, and forwards normalized virtual state into `PADSetVirtualStatus`. A held Space/A advanced the Metal title from action 3 through 4/5 and into K.K.'s new-game scene at the fixed cadence.
- Proved the complete ordered patch history from fresh detached worktrees: all 17 PC-port patches and all 4 Aurora patches apply cleanly at their pinned commits. No retail data entered either worktree or tracked artifacts.
- A deliberately parallel regression exposed that the patch runners' ignored append-only state files were not mutually exclusive; two fetchers could both apply a new tail patch. Restored the affected ignored checkout and added atomic per-checkout `mkdir` locks with bounded waits to both fetchers so parallel builds serialize patch mutation.
- Compared Aurora's K.K. scene directly with the OpenGL oracle and traced the neon/red output to host endianness rather than texture decoding. `EmuColor.raw` stores RGBA by numeric significance, but reading the union's byte fields on Apple ARM64 reversed the channels. Explicit Aurora-side unpacking restored the black stage, neutral bubble, white character, and green nameplate.
- Traced the absent dialogue body to a geometry-path split: the visible name already used polygon glyphs, while message text used the legacy N64 texture-rectangle path. Patch 18 selects polygons for Aurora and restores the identity font model-view after drawing the transformed window. Runtime captures show the complete opening three-line prompt and later two-line prompt correctly placed while input advances at 60.0 FPS.
- Rebuilt the native Aurora/Metal game and proved the complete ordered series from a fresh detached worktree: all 18 PC-port patches and all 4 Aurora patches apply cleanly. Added `settings.ini` to the root ignore policy after the convergence executable generated its local desktop configuration.
- Audited the touch-to-game seam before linking it and rejected the first push-callback design: Aurora's virtual PAD storage is unguarded, so UIKit callbacks would race the game thread's `PADRead`. Replaced it with canonical GameCube button bits plus a mutex-protected C snapshot on the product side and patch 19's game-thread poll with a weak standalone fallback.
- Rebuilt the macOS and iOS Simulator shells; the native unit test verifies all twelve GameCube masks, eight-byte state layout, source merge, snapshot copy, invalid-output rejection, and 10,000 concurrent writer/reader iterations without a torn state. Rebuilt `BellpadAurora`, confirmed its weak fallback and poll symbols while the iOS executable exports the strong snapshot, and reached the 60 Hz title loop with the unlinked fallback active.
- Ran the required simulators sequentially: booted/installed/launched iPhone 17 Pro, visually verified the full landscape touch set and exercised A, terminated and shut it down, then repeated on the iPad Pro 13-inch resizable layout. The iPad app also accepted A without a crash; it was terminated and shut down, leaving no simulator booted. No retail data was selected or packaged.
- Proved the complete ordered patch history from a fresh detached worktree: all 19 PC-port patches and all 4 Aurora patches apply cleanly at their pinned commits.
- Linked the complete Aurora game core into a universal native iOS/iPadOS `Bellpad.app`. SDL3 retains `UIApplicationMain`, UIKit window/lifecycle, and the Metal surface; Bellpad attaches a transparent adaptive GameCube overlay rather than creating a second renderer.
- Kept simulation fixed at 60 Hz and rejected 120/unlimited launch requests. The mobile plist and SDL orientation policy request landscape without forcing a competing UIKit geometry transition.
- Launched the real game sequentially on iPhone 17 Pro and iPad Pro 13-inch simulators using private ignored data copied only into each simulator sandbox. Both loaded 14,495 assets, opened SDL3 audio, and rendered the animated title through Aurora/Metal; all temporary simulator disc copies were removed afterward.
- Exercised the iPhone UIKit A button end to end. A very short accessibility tap exposed a missed-edge risk, so the normalized input mixer now latches rising button bits until one game-thread snapshot consumes them. The unit test covers that contract, and the repeated tap advanced from title into K.K.'s opening.
- Added patch 20 and `scripts/build-aurora-game-ios-simulator.sh`; the bundle audit proves ARM64 IOSSIMULATOR/Metal linkage and rejects packaged disc/save formats. A fresh detached worktree round-trip proves all 20 PC-port patches apply cleanly.
- Reassessed the mobile product rather than treating resident-count automation as a completion proxy. The current priority order is native user data, saves, lifecycle/text, representative gameplay, then deeper telemetry; resident count remains optional diagnostic evidence.
- Connected the real iOS/iPadOS entry point to a native Files importer. It validates the security-scoped source, stages and revalidates a private Application Support copy, atomically installs it without replacing valid data on failure, and supplies that path to Aurora's disc reader. A retained valid copy skips Files on relaunch; development `--disc` remains an explicit test bypass.
- Found that SDL3's traditional UIKit entry invokes the game main function on the main thread. A semaphore-only wait produced a black, unresponsive first-run app, so the importer now pumps the default and UI-tracking run-loop modes while awaiting modal completion. The corrected build visibly presents and dismisses Files without introducing a second app delegate or renderer.
- Exercised the native user path on iPhone: no-data screen, Files cancellation, synthetic invalid-image error, valid Files selection, byte-exact private copy, no staging residue, real Metal title boot, and retained no-argument relaunch all passed. UIKit A had already proven title-to-K.K. input on the same product.
- Stopped and shut down iPhone before booting iPad. The iPadOS managed-window presentation, Files import, byte-exact retained copy, native title with iPad control metrics, and retained no-argument relaunch all passed. Exact simulator test copies were removed or moved to Trash, the iPad was shut down, and no simulator remained booted.
- Added patch 21 for the validator/framework linkage and Files-to-retained-boot handoff. Live reverse-check and a fresh detached-worktree reverse/apply round-trip pass for the incremental patch.
- Rebuilt the universal ARM64 iOS Simulator product through the tracked script after patch 21. Mach-O/platform/plist, Metal linkage, SDL2 exclusion, shell syntax, tracked-content, and bundle-content audits pass; the app bundle contains only `Bellpad` and `Info.plist`.
## 2026-08-04

- Re-centered the mobile milestone around actual app usability rather than optional resident telemetry: import, touch/controller input, native text, town creation, save/relaunch, lifecycle, and representative gameplay are the critical path.
- Added a native UIKit text proxy that follows the real game editor lifecycle. UIKit queues UTF-8, Backspace, and Done through a bounded mutex buffer, while patch 22 drains it on the game thread into the existing editor API; UIKit never writes game state directly.
- Exercised player naming sequentially on iPhone 17 Pro and iPad Pro 13-inch simulators. The iPhone field became first responder, committed exact `Bell`, exited on Done, and Rover rendered `Bell`; iPad proved native field presentation, commit, editor exit, and dialogue echo, with exact full-name automation limited by Simulator host-focus pausing.
- Reoriented the remaining simulator work around product completion rather than repeated gameplay automation. The normalized controls are considered implemented after unit coverage plus representative live A/stick evidence; exhaustive activity-by-activity input replay moves to the acceptance plan.
- Added explicit native Done and paste handling to the UIKit editor proxy. A live iPhone pass committed exact `Bell` and `Cove` through the production game-thread queue without debugger injection; the universal simulator bundle rebuild passes.
- Hardened canonical GCI commits with checked flush/sync/close, atomic rename, three rolling backups, recovery, and Apple/POSIX parent-directory synchronization. Connected Aurora pause/unpause events to SDL3 audio pause/resume without triggering out-of-band gameplay saves.
- Rebuilt the macOS ARM64 Aurora executable and universal iOS/iPadOS Simulator app, then replayed all 24 PC-port patches cleanly from pinned commit `915fb86…` in a fresh detached worktree.
- Added a compact native touch-settings panel. Separate iPhone/iPad profiles persist opacity, global size, manual visibility, and normalized safe-area control centers; Move mode disables touch gameplay input while dragging, reset restores the current profile, and physical-controller auto-hide no longer hides the settings affordance.
- Rebuilt and launched the real Metal game for one bounded sequential UI smoke. The iPhone gear opened the complete settings panel with both sliders, both switches, and reset laid out correctly; after shutdown, the iPad profile rendered expanded controls in its managed window and opened the same panel at the appropriate scale. No gameplay progression was used as a proxy. Both exact private simulator ISO copies were moved to Trash and both simulators were shut down afterward.
- Made the native field width adaptive for resizable iPad windows and enabled the default international keyboard so the core's supported accented-character mapping remains reachable. Removed or trashed only the exact temporary simulator data copies, shut down iPad, and confirmed no simulator remained booted.
- Added patch 25 to select Aurora's pinned prebuilt Dawn package for iOS devices while retaining source Dawn for Simulator. The complete game, Aurora GX/Metal renderer, SDL3, UIKit controls/settings/import/keyboard adapter, and product metadata linked as an unsigned ARM64 iOS 17 device app.
- Added tracked device-build and unsigned-IPA scripts. The build audit verifies platform `IOS`, ARM64, Metal, plist validity, SDL2 exclusion, and absence of retail/save/provisioning files. The package audit strips signing material from staging and rejects disc images, extracted assets, saves, credentials, keys, certificates, and provisioning profiles in both the app and archive.
- Packaged the device app twice after timestamp normalization. Both 7.9 MB outputs were byte-identical with SHA-256 `9834e5171202139fdb656cf5938f6719146c34fe9994d4edac7e1af7f1f2a1ac`; each archive contained exactly `Payload/Bellpad.app/Bellpad` and `Payload/Bellpad.app/Info.plist`.
- Rejected and removed the first icon draft before it entered Git. Branding is now explicitly deferred until mobile town/save/relaunch, lifecycle, and representative-rendering gates are honest.
- Added a persisted Native/1×/2× render-resolution selector to the mobile settings panel and patch 26 to apply it through Aurora on the game thread. A bounded iPhone runtime check measured internal/native framebuffer pairs of 2622×1206/2622×1206, 1044×480/2622×1206, and 2087×960/2622×1206 without changing the fixed 60 Hz simulation cadence.
- Rebuilt the ARM64 iOS device app and packaged it twice. Both audited 7.9 MB outputs were byte-identical with SHA-256 `3200a8793c435cd16cad3f262720acb3b61be144923d0968774fb38c625f22a2`; the archive still contains only the executable and plist.
- Reoriented wrapper validation around representative iPhone/iPad input and settings evidence rather than repeated gameplay progression. The complete control surface remains wired through one normalized state; exhaustive activity scenarios remain in release acceptance.
- Expanded the top-right gear panel to Native plus 1×/2×/3×/4× render choices and adjusted Auto Layout so the Render label and all five segments remain readable in the iPad managed window. Runtime logs proved real 3×/4× internal framebuffers on iPhone and 4× on iPad without changing the 60 Hz simulation.
- Rebuilt the universal simulator and ARM64 device products. Two audited normalized IPA packages were byte-identical with SHA-256 `aebad5e8ec87de3049d8044449679a03faa3a13d7513c76c9b385b744d868d7b` and still contain only the executable and plist.
- Replaced the rejected icon direction with an original, full-bleed brass handbell and abstract folk-art sunburst generated from a text-only brief. No retail image, official artwork, character, logo, or third-party icon was used as input; the clean source constraints and provenance are tracked beside the asset.
- Added deterministic Apple icon integration. Mobile builds validate the opaque 1024×1024 source and compile iPhone/iPad renditions, plist metadata, and `Assets.car` through `actool`; the macOS build derives the complete iconset and emits `Bellpad.icns` from the same source. An installed iPhone Simulator build displayed the expected Bellpad icon and label on the home screen.
- Rebuilt the playable macOS app, universal iOS/iPadOS Simulator app, and ARM64 iOS device app with the original branding. Two timestamp-normalized 13 MB IPA packages were byte-identical with SHA-256 `3599d9cb757bcccfb706e2225b506f06472adf436963497587d28d68796e6064`; the archive contains only the executable, plist, two compiled icon PNGs, and `Assets.car`.
- Updated the guarded desktop QA helpers to recognize the packaged `Bellpad.app` executable as well as the historical binary name. In an isolated ignored profile, the packaged OpenGL app completed Bell/Cove setup and town initialization; the live game-facing save routine returned success and wrote a 467,008-byte `GAFE01`/`DobutsunomoriP_MURA` GCI.
- Closed that macOS game through its normal window control and observed `graph_proc` return with process status 0. A fresh process found the GCI, passed `Save_t`, `keep_mail`, `keep_original`, and `keep_diary` endian round trips, logged successful load, and entered the saved-player Cove station flow.
- Sequentially booted only iPhone Simulator, installed the existing universal product, and placed the local development ISO plus generated GCI only in that app's private container. The native Aurora/Metal core loaded the save, the same live save routine returned success, `.bak1` preserved the original, and a changed 467,008-byte canonical GCI loaded successfully after app termination/relaunch.
- The iPhone run also exposed repeated SDL UIKit `Unbalanced calls to begin/end appearance transitions` warnings, now promoted to the next lifecycle hardening item. The app was uninstalled, the simulator shut down, the exact private data container disappeared, and no simulator remained booted.
- Reproduced the two startup appearance warnings on a direct-disc iPhone launch. A dependency experiment that skipped SDL's second root-controller handoff removed the warnings but returned to a black view after foregrounding, so it was rejected and the fetched SDL source restored.
- The unmodified SDL/Aurora path restored Metal presentation after Home, but LLDB proved the SDL3 audio stream stayed paused. Patch 27 adds a UIKit `DidBecomeActive` edge consumed on the game thread after Aurora events; a bounded retest measured the stream false before Home, true in the background, and false after foreground while live title rendering resumed.
- Rebuilt the universal Simulator app and ARM64 iOS device app with patch 27. All 27 PC-port patches applied cleanly in a fresh detached worktree. The final two-edge lifecycle build produced two audited, byte-identical unsigned IPA packages with SHA-256 `12273ffce1f06214433bd54f64104501e5a78b588ccbce3efd1614ebdc29e55c`.
- Sequentially launched the rebuilt universal product on iPad Pro 13-inch Simulator after shutting down iPhone. The real title rendered through Metal at a 2752×2064 drawable with the persisted 4× 2560×1920 internal framebuffer and live SDL3 audio. iPadOS kept the managed app scene `UIApplicationStateActive` when Simulator Home exposed the desktop, so that was correctly recorded as a managed-window limitation rather than claimed as a second background transition. The app was uninstalled, iPad shut down, and no simulator remained booted.
- Zoomed out around the product finish line: controls remain accepted through unit coverage plus representative iPhone/iPad use, while remaining work is app/runtime and release hardening rather than exhaustive button-by-button gameplay replay.
- Added a clean shared GCI validator with synthetic unit coverage for valid data, checksum corruption, unsupported version, invalid town ID, and wrong size. Patch 28 links it into the native Apple game target.
- Added `Game Data & Saves…` to the top-right gear panel. Native Files actions export an immutable validated GCI snapshot, stage validated imports for installation before the next core startup with a pre-import backup, and let the user change/reimport or remove retained game data without touching saves. Staging and installation synchronize file and directory metadata.
- Ran one bounded sequential UI smoke: iPhone showed the scrolling compact panel, the correct no-save export alert, and native import picker; after termination, uninstall, and shutdown, iPad showed the full panel and native picker in its managed window. Both launched the real Metal title using an ignored host-only development path; no retail image was copied into either simulator, and no simulator remained booted.
- Rebuilt the universal Simulator and ARM64 device products, replayed all 28 core patches from the pinned commit in a fresh detached worktree, and passed the synthetic validator/unit suite. Two audited 13 MB unsigned IPA packages were byte-identical with SHA-256 `03e362731837c944317d50fafee81ea15aca5632985529c64d46c8bc64dd567b`; the archive still contains only the executable, plist, two compiled icon PNGs, and `Assets.car`.
- Proved isolated Dolphin interoperability without touching the user's normal Dolphin profile: Dolphin 5.0-17995 booted GAFE01 with Bellpad's 467,008-byte Bell/Cove GCI in a private GCI Folder and reported no invalid-header/data diagnostics. Bellpad then installed and loaded that Dolphin-managed copy before mobile startup; the SHA-256 remained `3f4fbf9f93001ddc395ce264ce522e1fe5f70d9948811eb7bdd28de2bd4a59ae`.
- Used the Computer Use workflow for a bounded real-UI pass rather than exhaustive button replay. iPhone returned through the saved-player dialogue and train to Cove station, then the production gear-menu export opened Files, wrote a byte-identical GCI, dismissed, and returned to live Metal rendering. After iPhone shutdown, iPad visibly exposed Native/1×/2×/3×/4×, applied a scale choice, exported the same GCI through its managed Files sheet, and returned to the game.
- Fixed a simulator Metal trap exposed by mid-game document presentation. Because Aurora's SDL entry runs on UIKit's main thread, save pickers now pump UIKit while deliberately holding the render loop, then wait for automatic dismissal before resuming Metal. The phone and tablet export flows completed without the crash and removed the temporary snapshot.
- Rebuilt the simulator and ARM64 device products, reran the native input/GCI-validator suite and tracked-content audit, and packaged twice. Both audited unsigned IPAs were byte-identical with SHA-256 `39af9b94fb929314c07adc4095c074517984fd0d934bd7cef7fec058d2d44742`.
- Replaced permissive raw-image header-only acceptance with exact full/trimmed size allowlisting and a small streamed SHA-256 implementation over the complete meaningful payload. Synthetic tests cover both accepted container lengths plus wrong size/hash/revision/game; the optional ignored retail-image integration passes without adding a fixture.
- Exercised the stricter validator through the real Files flow on a fresh iPhone Simulator and through retained-image startup on a fresh iPad Simulator. Both reached the native Metal product; the temporary simulators and their private retail copies were deleted afterward.
- Rebuilt the universal Simulator and ARM64 device products after the fingerprint change. Two audited 13 MB unsigned IPA packages were byte-identical with SHA-256 `58917d934a54595c45b099f5d5f7e13623059265e95969a847aa9bbe1182423e`.
- Ran a release-oriented clean-checkout audit instead of expanding control acceptance. It exposed an `@rpath/libpng16.16.dylib` dependency and absolute build-directory `LC_RPATH` that the simulator could satisfy from the host but a physical device could not. Mobile builds now force static third-party linkage, and both build and packaging scripts reject every non-system dynamic dependency and runtime search path.
- Mapped checkout-local compiler paths to `/bellpad` for location-independent output. The working tree, a repeated local package, and an independent fresh GitHub clone now produce the same 27,904,736-byte device executable and the same audited unsigned IPA: SHA-256 `b0a9ff6f405a241a90f9ade71b5c54fd999922533fa3887f29bc736e6baf6e98`.
- Rebuilt the complete universal Simulator product with the static-link policy. A bounded sequential smoke installed and launched the native no-data/Files screen on iPhone 17 Pro and then iPad Pro 13-inch; each process was terminated and its simulator shut down. No redundant activity-by-activity button replay was performed.
- Audited the actual ARM64 device link command instead of relying on declared dependencies. Added a machine-readable lock for every linked non-system component, full notices for the game core, bc7decomp, Aurora, Dawn/Tint, Abseil, SDL3, fmt, FreeType, libpng, Dear ImGui, xxHash, and Tracy, plus a mixed-license root notice that does not invent rights for Bellpad-authored files.
- Added Aurora patch 5 to SHA-256 verify the formerly version-only Abseil, SDL3, Dawn source, iOS Dawn, and macOS Dawn archives. Rebuilt the complete device and Simulator products through those pins. Product builds install the exact notice into macOS/iOS/iPadOS bundles, and build, staging, and archive audits reject a missing or changed copy.
- Packaged the notice-bearing ARM64 device app twice. Both normalized, ROM-free 13 MiB IPAs contain only the executable, plist, asset catalog, two compiled icons, and 48,187-byte notice, with SHA-256 `2a925959b5e9a5f96d61038715802c26d078cecf9e4bfdec3deb90655bb6d35d`.
- Inspected the actual SDL and Dawn static archives. The Metal/Tint Dawn package contains Abseil but no Vulkan, DXC, SPIR-V Tools, or protobuf object code; SDL does compile HIDAPI and yuv2rgb, so their separate BSD-3-Clause notices are now locked, audited, and bundled. Two final 51,689-byte-notice IPAs reproduced at SHA-256 `a23c0d5c3c888f9b7db1adb5b09aaff8c242ccb293a70483ea79432730dd4ac2`.
- Ran the requested zoomed-out simulator/runtime audit without expanding button acceptance. A fresh saved-GCI iPhone run rendered the animated title water and choice UI through Metal and held roughly flat physical memory over a bounded four-minute sample after shader warmup. Repeated Home/foreground testing then exposed a real second-cycle `EXC_BAD_ACCESS` in `aurora::gfx::get_render_target_size`: `JW_BeginFrame` ignored Aurora's false result while the app lacked a presentable surface, so `JW_EndFrame` drained GX commands without an active frame packet.
- Added core patch 29 so the JSystem/Aurora bridge records a declined frame, discards that skipped frame's software FIFO, pumps retrace/lifecycle events, and never calls `aurora_end_frame` without a matching begin. The rebuilt universal Simulator product survived three iPhone Home/resume cycles with three matched audio pause/resume pairs and restored Metal output, then survived three sequential iPad Home/resume cycles with visible rendering and no new crash report. The dedicated simulator devices and their private retail/save copies were deleted afterward.
- Replayed all 29 core patches from pinned commit `915fb86…`, passed the native input/GCI-validator suite and tracked/release audits, and relinked the macOS Aurora and ARM64 iOS device products. Two audited timestamp-normalized unsigned IPAs reproduced byte-for-byte at SHA-256 `f71b02a2890b3695e35673f5070270a2ef04490c8bd6d829092ed5bf5c83d864`.
- Audited the remaining product gaps instead of expanding control acceptance. The existing RTC used one whole-second wall-clock sample at launch and then advanced only from a performance counter, so device clock and timezone changes during a long or suspended session were invisible to the game.
- Added core patches 30/31 and a deterministic retail-data-free RTC suite. The clock now uses a subsecond wall-clock anchor plus overflow-safe absolute performance-counter conversion, polls for drift, and rebases on iOS activation, significant-time-change, and timezone-change edges consumed on the game thread. Epoch, offset, time-override, large/zero-frequency counter, and nanosecond cases pass.
- Rebuilt the universal simulator product and ran one bounded sequential release smoke. The real retained GAFE01 game booted on iPhone through Metal with the existing GCI and 32 kHz audio; Home/resume logged matched audio edges and `RTC synchronized ... adjustment 0.000 seconds`. After uninstalling and shutting down iPhone, the same bundle booted the real game on iPad, returned from one Home/resume cycle, and was uninstalled. No simulator remained booted and the private simulator copies were removed.
- Replayed patches 30/31 against a fresh pinned worktree and byte-compared every RTC-touched core source with the working patched checkout. Added a source-release verification command covering tracked/release audits, native platform/input/disc/GCI tests, RTC tests, shell syntax, and whitespace checks, then refreshed public build/status/testing/legal-facing documentation for a cross-machine release-candidate handoff.
- Rebuilt the complete unsigned ARM64 iOS 17 device product with patches 30/31, static-link/runtime-path audits, compiled icon assets, and exact notices. A second normalized package from the audited app reproduced byte-for-byte at SHA-256 `a9e85a86a9cc2956ab0d161080b418bb9d358b692551e9cba21d9fb167914075`; the IPA remains ignored and contains no game image, save, signature, provisioning profile, credential, or private key.
